def duplicate_encode(word):
    list_of_char = list(word.lower())
    strng = ""
    for i in word.lower():
        if list_of_char.count(i) == 1:
            strng = strng + "("
            
        else :
            strng = strng + ")"
            
    return strng