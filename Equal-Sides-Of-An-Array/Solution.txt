def find_even_index(arr):
    i = 0
    while i < len(arr):
        if sum(arr[0:i]) == sum(arr[i+1:]) :
            return i 
        else :
            i = i + 1
            continue
            
    return -1